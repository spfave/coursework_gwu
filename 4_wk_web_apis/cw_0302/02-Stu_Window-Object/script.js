// TODO: What will the following line of code log?
console.log(window); // will console log the web browser window object

// TODO: What will the following line of code log?
console.log(window.document); // will console log the html DOM (object model representation of the html tags). Includes DOCTYPE tag and entire html tag and its contents

// TODO: What will the following line of code log?
console.log(document.documentElement); // will console log the root element (the html tag and all its contents)

// TODO: What will the following line of code log?
console.log(document.head); // will console log the head tag element and all its content

// Bonus
console.log(window.length);
